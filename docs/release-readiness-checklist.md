# CareRecord 提供開始チェックリスト

最終更新: 2026-07-16

## 判定方法

- Criticalは受容せずすべて完了させる。Highは原則完了させ、理由、代替策、承認者を記録した`accepted-risk`だけを例外とする。提供条件は「Critical 0件、未解決かつ未受容のHigh 0件」とする。再評価日・有効期限はaccepted-riskの必須項目にせず、未設定・経過だけでGo判定を拒否しない。
- `実装済み`だけでは完了としない。テスト結果、本番設定、復元結果等の証跡リンクを残す。
- `N/A`は理由と承認者を記録する。
- MFA未導入、Supabase Free利用、パスワード8文字、長時間セッションは承認済みの既知リスクだが、代替策の実装確認は省略しない。

## Critical: 認証・認可・テナント分離

- [ ] 全publicテーブル、View、RPC、Storage/GCSアクセス経路の一覧がある
- [ ] exposed schemaの全テーブルでRLSが有効である
- [ ] 通常業務のServer Action/APIがservice roleを使っていない
- [ ] service role利用が承認済みallowlistと一致し、用途・対象・入力認可・監査が登録されている
- [ ] allowlist外の`supabaseAdmin`利用をCIが拒否する
- [ ] 未ログイン、一般、管理、オーナー、super admin、無効化ユーザーの拒否テストがある
      （2026-07-19実装: `src/app/actions/clients.test.ts`／`src/app/actions/super-admin.test.ts`
      でServer Action層の未ログイン・一般・オーナー・super_adminの拒否/防御チェックを追加。
      accepted-risk: 「無効化ユーザー」はis_active的なカラムがprofilesに存在せず
      DB/認可レイヤーに機構自体がないため今回は対象外。理由: RLS/permissions.tsへの
      新規導入を要する別スコープの変更。代替策: 招待失効・ロール剥奪など既存の
      権限剥奪経路で運用回避。承認者: [記入してください] / 記録日: 2026-07-19。
      `npm run test:unit`実行結果をここに証跡リンクすること）
- [ ] 同一組織、別組織、担当、未担当、削除済み、存在しないIDの拒否テストがある
      （2026-07-19実装: `src/app/actions/clients.test.ts`で別組織ID・論理削除済み・
      存在しないIDに対する`assertClientOrg`の拒否を検証。担当/未担当（shift割当）の
      拒否テストは未着手）
- [ ] UI、Server Action、API、Supabase REST/RPC、Supabase Storage署名URLで同じ認可結果になる（提供後のGCS正本化時はGCS署名URLも対象）
- [ ] クライアント入力のuserId、role、organizationIdを信頼していない
- [ ] super adminがアプリ内のどの経路からも顧客の記録本文・ファイルを取得できない（2026-07-16決定: 緊急アクセス機構は実装せず、基盤側運用＋手動記録で代替）
- [ ] 基盤側緊急アクセスの前後記録が削除不能なGCS監査bucketへ追記される
- [ ] 基盤側アクセスの非強制連動が理由・代替策・承認者付きのaccepted-riskへ登録されている
- [ ] 最後のロール管理者・オーナーを失う操作が拒否される
- [ ] 招待のメール一致、72時間期限、1回利用、取消、再発行失効を確認した

## Critical: 記録の真正性と整合性

- [ ] 複数テーブル更新がトランザクションまたは原子的RPCになっている
- [ ] 記録の作成・承認に冪等性があり、通信再送で二重登録されない（2026-07-16決定: 対象を記録系に限定）
- [ ] 楽観ロックにより同時編集の後勝ち上書きが起きない
- [ ] 承認済み記録を通常更新・通常削除できない
- [ ] 訂正が理由付きの新しい版として保存される
- [ ] 版ハッシュと監査ハッシュチェーンを検証できる
- [ ] 作成、訂正、承認、差戻し、復元、出力を一連で再現できる

## High: セッションとアカウント

- [ ] 無操作24時間、絶対30日がブラウザ、middleware、Server Actionで一致する
- [ ] 失効、ログアウト、パスワード変更、メール変更、退会後に既存セッションを利用できない
- [ ] 再認証対象操作だけが再認証を要求し、対象外操作へ誤適用されていない
- [ ] IP・アカウント単位のログイン制限を回避できない
- [ ] エラーメッセージから登録済みメールを列挙できない
- [ ] パスワード8文字以上とパスワードマネージャー対応を確認した
- [ ] MFA未導入の受容理由、代替策、承認者が記録されている

## High: 環境分離と秘密情報

- [ ] Developmentはローカル、Staging/Productionは別Supabase Projectである
- [ ] Vercel、GCS、OAuth、鍵、Cron、監査、バックアップが環境ごとに分離されている
- [ ] 非本番からProductionへのネットワーク・認証接続ができない
- [ ] Stagingに本番個人情報がない
- [ ] 必須環境変数の欠落・別環境値でビルドまたは起動が失敗する
- [ ] service role、GCP資格情報、暗号鍵がブラウザbundle、ログ、リポジトリにない
- [ ] 鍵の緊急交換と旧鍵復号の試験を実施した

## High: 記録ファイル（初期提供はSupabase Storage正本）

- [ ] 署名URLが対象1件、5分、認可後にだけ発行される
- [ ] 別組織・未担当者がURLを取得できない
- [ ] JPEG/PNG/WebPだけを受け付け、MIME・シグネチャ・実デコード結果が一致する
- [ ] 10MB・最大画素数・件数上限がサーバーで検証される
- [ ] metadata除去とWebP再エンコード後のデータだけを保存し、元ファイルを残さない
- [ ] PDF/HEICの記録添付が提供されていない（未実装のまま公開されていない）
- [ ] プレビュー・ダウンロードが監査される
- [ ] マルウェア検査未導入が理由・代替策・承認者付きのaccepted-riskへ登録されている

## High: キャッシュ（初期提供はPWAなし・オンライン専用）

- [ ] Service Workerが登録されない（既存SWの除去処理が動作する）
- [ ] 認証後HTML・API応答が`private, no-store`である
- [ ] ログアウト後の戻る操作で認証後画面を再表示しない

## High: バックアップと復旧

- [ ] DB完全論理バックアップが12時間ごとにGitHub Actionsで成功し、gzip圧縮されている（Supavisorセッションプーラー経由）
- [ ] 失敗時の30分後・2時間後の再試行と手動実行が動作する
- [ ] 最新成功世代と対象データ最大更新時刻が14時間以内である
- [ ] roles、schema/RLS/関数、業務data、Auth、Storage metadata、migration、設定version、鍵ID、manifestが同一世代に揃っている
- [ ] 直近24時間はStandard、日次35日はNearline、月次370日はArchiveで保持されている
- [ ] Supabase Storage画像が保存後にGCSへ複製され、失敗が再試行される
- [ ] 日次の画像inventory照合で件数・サイズ・ハッシュ・関連ID・削除/hold状態が一致し、最新の完全同期が26時間以内である
- [ ] GCSファイルの世代管理と大阪の別bucket複製を確認した
- [ ] 監査ログがDBバックアップとは別系統へ追記保全される
- [ ] バックアップの復号、件数、ハッシュ、欠落を週次で検査している
- [ ] 提供前および月次のStaging復元でDBと画像の関連を含めRPO 14時間、RTO 4時間を実測した
- [ ] RTO適用条件とリージョン・複数クラウド障害のbest-effort範囲が証跡に記録されている
- [ ] Stagingを緊急復旧先へ切り替え、その後再構築する手順を実演した
- [ ] 復元環境でメール、Google、AIが無効になる
- [ ] Auth全損時のユーザー復元とパスワード再設定を試験した
- [ ] バックアップ失敗時の通知と新規デプロイ停止を確認した

## High: 削除と保持

- [ ] Productionのpurgeが既定でdry-runになる
- [ ] URLパラメータ、HTTP method、Cron、アプリ内service role経路から物理削除できない
- [ ] 通常削除が論理削除で、記録・GCSファイルを自動物理削除しない
- [ ] legal holdが削除より優先される
- [ ] アプリ外の個別物理削除手順で対象、承認、legal hold、結果、不整合を監査できる
- [ ] アカウント削除申請時にセッション、所属、外部資格情報が失効する

## High: 監査・監視

- [ ] 権限変更、承認、削除、復元、出力、ファイル閲覧、緊急アクセスを記録する
- [ ] 成功だけでなく拒否・失敗も記録する
- [ ] ログに記録本文、ファイル、パスワード、トークン、秘密情報がない
- [ ] アプリ内1年、外部10年の保持設定を確認した
- [ ] ハッシュチェーンを週次で検証し、改ざんテストで通知される
      （2026-07-19実装: `public.verify_audit_chain()`（`supabase/migrations/20260719102606_audit_chain_verification.sql`）
      が`private.chain_audit_event()`と同一の正規化式でチェーンを再検証し、
      `src/app/api/cron/verify-audit-chain/route.ts`経由で`.github/workflows/audit-chain-verification.yml`
      （毎週月曜03:30 UTC）が呼び出し、失敗時は`notify-discord.sh`でCritical通知する。
      未実施: staging環境での実行確認と、行を改ざんして検知することの実地確認
      （ローカルDockerが使えない環境で作業したため`supabase start`での検証ができていない）。
      証跡: staging環境で本ワークフローを一度dispatchし、正常時とタンパー時の実行結果リンクをここに残すこと）
- [ ] 別組織アクセス成功、認証回避、監査改ざんで自動隔離される
- [ ] 5分間隔の外形監視が個人情報を読まないDB health queryまで確認する
- [ ] Supabase Freeの自動一時停止・復旧遅延が理由・代替策・承認者付きのaccepted-riskへ登録されている
- [ ] DB容量300/400/450MBの警告・停止を試験した

## High: 外部連携と障害時動作

- [ ] AI機能フラグがProductionで無効である
- [ ] Google障害時にDB保存が成功し、未同期から再開できる
- [ ] Google同期が同時1件のキューで動作する
- [ ] 監査障害時に重要操作を拒否する
- [ ] バックアップ障害時は業務継続し、通知される
- [ ] 外部連携のタイムアウト、再試行、二重実行防止を確認した

## High: CI/CDと脆弱性

- [ ] lintが成功する
- [ ] TypeScript型チェックが成功する
- [ ] 単体、UI、アクセシビリティ、RLS、主要E2Eが成功する
- [ ] Production buildが成功する
- [ ] 空DBから全migrationを適用できる
- [ ] High/Critical依存脆弱性が0件である
- [ ] secret scanとSBOM生成が成功する
- [ ] Production workflowの無効化条件が残っていない
- [ ] 機能フラグ、限定配信、アプリrollbackを実演した

## UI・性能

- [ ] キーボード操作、コントラスト、入力エラー読み上げを主要業務で確認した（WCAG 2.2 AA全項目監査は正式提供時。2026-07-16決定）
- [ ] 幅360px、200%拡大で主要業務を完了できる
- [ ] 保存中、保存済み、未同期、失敗、競合が区別される
- [ ] 一覧・検索と保存がp95 2秒以内、主要表示3秒以内である
- [ ] 同時20セッションと利用者500人・記録10万件・シフト10万件相当で主要操作を計測した
- [ ] 試験データが容量安全弁を超えた場合、実収容件数へ設計目標を修正した
- [ ] ログイン試行・出力生成・ファイルアップロードのレート制限を試験した
- [ ] CSV式、ZIPパス、PDFメタデータの安全化を試験した

## 最終判定欄

- Critical未完了件数:
- 未解決かつ未受容のHigh件数:
- accepted-risk一覧・受容理由・代替策・承認者:
- 復元試験日時・RPO・RTO:
- 対象Production Project ID:
- 対象commit SHA:
- 判定: Go / No-Go
- 判定日時:
- 証跡リンク:

## 提供後ロードマップ（Go/No-Go集計対象外）

- [ ] 記録ファイルの正本を非公開GCSへ移行する
- [ ] PDF/HEIC添付と安全化処理を提供する
- [ ] マルウェア検査をfail-closedで導入する
- [ ] PWAの静的資産限定キャッシュ、暗号化下書き、オフライン確定防止、更新制御を提供する
- [ ] WCAG 2.2 AA全項目を監査する
- [ ] 有料基盤への移行、RPO 1時間、可用性目標99.5%以上を再評価する
