# バックアップ・復旧・BCP

## 目標

暫定目標はRPO 60分、RTO 8時間です。医療・介護業務への影響評価と顧客合意後に確定し、SLAと一致させます。

## 必須構成

- Supabase PITRまたは同等機能を有効化し、DB、Storage、Auth、設定を復旧対象にする
- 本番管理アカウントと論理分離された別アカウント／保管先へ暗号化バックアップを保存する
- ランサムウェアを想定し、変更不能期間またはWORMを設定する
- 鍵、Migration、環境変数名、DNS、Vercel設定、Google連携復旧手順を同時に保全する
- バックアップ成功だけでなく、欠落・遅延・容量・改ざんを監視する

## 四半期復旧試験

隔離環境へ復旧し、件数、代表レコード、Storage、記録ハッシュチェーン、認証、組織分離を照合します。実測RPO/RTO、失敗、是正期限、承認者を`backup_restore_tests`へ記録します。本番データを試験に利用する場合は同等のアクセス制御と終了後消去を行います。

## 非常時運用

サービス停止時は利用事業所が承認済み紙様式または隔離された代替手段で最小限の記録を継続し、復旧後に入力者、作成時刻、転記者、転記時刻を明示して登録します。未承認の個人クラウド、私物メール、メッセージアプリは使いません。
